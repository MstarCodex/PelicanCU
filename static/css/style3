body {
    font-family: Arial, sans-serif;
    margin: 0;
    padding: 0;
    background-color: #f0f0f0;
}

header {
    background-color: #333;
    color: #fff;
    padding: 1rem;
    text-align: center;
}

#chat-container {
    width: 80%;
    margin: 2rem auto;
    background-color: #fff;
    padding: 1rem;
    border-radius: 10px;
    box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
}

/* Messages container */
#messages {
    height: 400px;
    overflow-y: scroll;
    border: 1px solid #ddd;
    padding: 10px;
    background-color: #f9f9f9;
    border-radius: 5px;
    margin-bottom: 15px;
}

/* Individual message styling */
#messages div {
    margin-bottom: 10px;
    padding: 8px;
    border-radius: 5px;
    position: relative;
    max-width: 75%;
}

/* Message from the user */
#messages div b {
    display: block;
    font-size: 0.9rem;
    color: #333;
}

#messages div .timestamp {
    display: block;
    font-size: 0.75rem;
    color: #999;
    position: absolute;
    bottom: 5px;
    right: 10px;
}

/* Different styling for user and admin messages */
#messages div:nth-child(odd) {
    background-color: #d1e7dd;
    text-align: left;
    align-self: flex-start;
    border: 1px solid #b2dfdb;
}

#messages div:nth-child(even) {
    background-color: #e2e3e5;
    text-align: right;
    align-self: flex-end;
    border: 1px solid #ced4da;
}

/* Input area */
#message-input {
    width: calc(100% - 100px);
    padding: 10px;
    border: 1px solid #ddd;
    border-radius: 5px;
    margin-right: 10px;
    font-size: 1rem;
    outline: none;
}

/* Send button */
#send-btn {
    padding: 10px 20px;
    background-color: #007bff;
    color: white;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    font-size: 1rem;
}

#send-btn:hover {
    background-color: #0056b3;
}

/* Chat container responsive for smaller screens */
@media only screen and (max-width: 600px) {
    #chat-container {
        width: 95%;
    }

    #message-input {
        width: calc(100% - 90px);
    }
}

/* Header styling */
header {
    background-color: #007bff;
    color: white;
    padding: 1rem;
    text-align: center;
    font-size: 1.5rem;
}

header button {
    background-color: #dc3545;
    border: none;
    padding: 8px 15px;
    color: white;
    font-size: 1rem;
    border-radius: 5px;
    cursor: pointer;
}

header button:hover {
    background-color: #c82333;
}